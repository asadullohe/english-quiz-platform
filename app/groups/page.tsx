import { PlaceholderPage } from "@/components/placeholder-page";

export default function GroupsPage() {
  return (
    <PlaceholderPage
      title="Guruhlar"
      description="Teacher group yaratadi, invite code beradi, support teacher biriktiradi."
      items={[
        "Create group server action",
        "Invite code regenerate",
        "Member list va support teacher assignment",
      ]}
    />
  );
}
