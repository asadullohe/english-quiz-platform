import { PlaceholderPage } from "@/components/placeholder-page";

export default function ReviewQuestionsPage() {
  return (
    <PlaceholderPage
      title="Savollar review"
      description="Pending submissions approve, edit, needs changes yoki reject qilinadi."
      items={[
        "Assigned group filtering",
        "Question editor with options/text answers",
        "Review events timeline",
      ]}
    />
  );
}
